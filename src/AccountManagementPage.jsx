import {useCallback,useEffect,useState} from 'react'
import PasswordInput from './PasswordInput.jsx'
import {useI18n} from './i18n.jsx'

const roles=['operations_admin','supervisor','office','driver','crew']
const api=async(path,options={})=>{const response=await fetch(path,{headers:{'Content-Type':'application/json'},...options}),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'Account request failed');return data}

export default function AccountManagementPage({account}){
  const{t}=useI18n(),[items,setItems]=useState([]),[employees,setEmployees]=useState([]),[selected,setSelected]=useState(null),[draft,setDraft]=useState(null),[password,setPassword]=useState(''),[createForm,setCreateForm]=useState({employeeId:'',username:'',role:'office',password:''}),[message,setMessage]=useState(''),[error,setError]=useState('')
  const owner=account.role==='owner_admin'
  const load=useCallback((selectedId=null)=>Promise.all([api('/api/auth/accounts'),api('/api/resources')]).then(([data,resources])=>{setItems(data.items);setEmployees(resources.employees.filter(employee=>!employee.accountId&&employee.employmentStatus==='active'));if(selectedId){const fresh=data.items.find(item=>item.id===selectedId);setSelected(fresh||null);setDraft(fresh?{username:fresh.username,role:fresh.role}:null)}}).catch(item=>setError(item.message)),[])
  useEffect(()=>{void load()},[load])
  const open=item=>{setSelected(item);setDraft({username:item.username,role:item.role});setPassword('');setMessage('');setError('')}
  const update=async payload=>{setError('');setMessage('');try{const result=await api(`/api/auth/accounts/${selected.id}`,{method:'PATCH',body:JSON.stringify(payload)});setSelected(result);setDraft({username:result.username,role:result.role});setPassword('');setMessage('Account updated and audit recorded.');await load(result.id)}catch(item){setError(item.message)}}
  const create=async event=>{event.preventDefault();setError('');try{await api('/api/auth/accounts',{method:'POST',body:JSON.stringify(createForm)});setMessage('Account created. The employee must change the temporary password at first login.');setCreateForm({employeeId:'',username:'',role:'office',password:''});await load()}catch(item){setError(item.message)}}
  return <div className="page account-management"><div className="data-title"><em>KCS SECURITY</em><h1>{t('nav.accounts')}</h1><p>System Role is separate from Primary Job Role. Owner Admin controls usernames and roles.</p></div>{error&&<div className="data-error">{error}</div>}{message&&<div className="import-message">{message}</div>}<form className="account-create" onSubmit={create}><h2>Create employee account</h2><label>Employee<select required value={createForm.employeeId} onChange={event=>setCreateForm({...createForm,employeeId:event.target.value})}><option value="">Select active employee</option>{employees.map(employee=><option key={employee.id} value={employee.id}>{employee.employeeCode} — {employee.name}</option>)}</select></label><label>Username<input required value={createForm.username} onChange={event=>setCreateForm({...createForm,username:event.target.value})}/></label><label>System Role<select value={createForm.role} onChange={event=>setCreateForm({...createForm,role:event.target.value})}>{roles.filter(role=>owner||role!=='operations_admin').map(role=><option key={role}>{role}</option>)}</select></label><label>Temporary password<PasswordInput minLength="8" value={createForm.password} onChange={event=>setCreateForm({...createForm,password:event.target.value})} required/></label><button>Create account</button></form><div className="account-layout"><section className="account-list">{items.map(item=><button key={item.id} className={selected?.id===item.id?'selected':''} onClick={()=>open(item)}><b>{item.employeeName}</b><span>{item.username} · {item.role}</span><small>{item.isActive?'Active':'Disabled'}</small></button>)}</section><section className="account-editor">{!selected?<p>Select an account.</p>:<><h2>{selected.employeeName}</h2><p>{selected.employeeCode} · Account ID {selected.id}</p><label>Username<input value={draft.username} disabled={!owner||selected.role==='owner_admin'} onChange={event=>setDraft({...draft,username:event.target.value})}/></label><label>System Role<select value={draft.role} disabled={!owner||selected.role==='owner_admin'} onChange={event=>setDraft({...draft,role:event.target.value})}>{selected.role==='owner_admin'&&<option value="owner_admin">owner_admin</option>}{roles.map(role=><option key={role}>{role}</option>)}</select></label>{owner&&selected.role!=='owner_admin'&&<button onClick={()=>update(draft)}>{t('common.save')}</button>}<div className="account-actions"><button onClick={()=>update({isActive:!selected.isActive})} disabled={selected.role==='owner_admin'}>{selected.isActive?'Disable':'Enable'}</button><button onClick={()=>update({unlock:true})} disabled={selected.role==='owner_admin'}>Unlock</button></div><form onSubmit={event=>{event.preventDefault();void update({password})}}><label>Temporary password<PasswordInput minLength="8" value={password} onChange={event=>setPassword(event.target.value)} required/></label><button disabled={selected.role==='owner_admin'&&!owner}>Reset password</button></form></>}</section></div></div>
}
