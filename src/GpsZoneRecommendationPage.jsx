import { useCallback, useEffect, useMemo, useState } from 'react'
import { CircleMarker, MapContainer, Polygon, Popup, TileLayer, useMapEvents } from 'react-leaflet'
import 'leaflet/dist/leaflet.css'
import './GpsZoneRecommendationPage.css'
import {kuchingDate} from '../shared/kuchingTime.js'
import {apiRequest as api} from './apiClient.js'
import {useI18n} from './i18n.jsx'

const colors=['#16866f','#3277ad','#c87a32','#745bb7','#a45161','#287d89','#8b6b35','#516d7d','#b14e42']
const today=kuchingDate

export default function GpsZoneRecommendationPage({currentUser}){const{t}=useI18n(),[tab,setTab]=useState('recommendations'),[resources,setResources]=useState(null),[boundaries,setBoundaries]=useState(null),[recommendations,setRecommendations]=useState(null),[error,setError]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false)
  const load=useCallback(async()=>{try{const[resourceData,boundaryData,recommendationData]=await Promise.all([api('/api/resources'),api('/api/zone-boundaries?history=true'),api('/api/gps-zone-recommendations')]);setResources(resourceData);setBoundaries(boundaryData);setRecommendations(recommendationData);setError('')}catch(item){setError(item.message)}},[]);useEffect(()=>{load()},[load])
  const act=async(url,body)=>{setBusy(true);setError('');setMessage('');try{await api(url,{method:'POST',body:JSON.stringify({...body,changedBy:currentUser.name,confirmedBy:body.confirmedBy||currentUser.name})});setMessage(t('gps.recommendationSaved'));await load()}catch(item){setError(item.message)}finally{setBusy(false)}}
  return <div className="page gps-zone-page"><div className="data-title"><em>GPS-BASED ZONE RECOMMENDATION V1</em><h1>{t('gps.zoneTitle')}</h1><p>{t('gps.zoneDescription')}</p></div>{message&&<div className="planner-message">✓ {message}</div>}{error&&<div className="data-error">{error}</div>}<div className="gps-zone-tabs"><button className={tab==='recommendations'?'active':''} onClick={()=>setTab('recommendations')}>{t('gps.supervisorReview')}</button><button className={tab==='boundaries'?'active':''} onClick={()=>setTab('boundaries')}>{t('gps.boundaryMap')}</button></div>{!resources||!boundaries||!recommendations?<div className="data-loading">{t('gps.loading')}</div>:tab==='boundaries'?<BoundaryManager resources={resources} data={boundaries} busy={busy} act={act}/>:<RecommendationManager resources={resources} boundaries={boundaries} data={recommendations} busy={busy} act={act} t={t}/>}</div>}

function MapClicker({enabled,onPoint}){useMapEvents({click:event=>{if(enabled)onPoint({lat:Number(event.latlng.lat.toFixed(7)),lng:Number(event.latlng.lng.toFixed(7))})}});return null}
function BoundaryLayers({items}){return items.filter(item=>item.isActive&&item.polygon?.length).map(item=><Polygon key={item.id} positions={item.polygon.map(point=>[point.lat,point.lng])} pathOptions={{color:colors[(item.zoneGroupId-1)%colors.length],weight:2,fillOpacity:.12}}><Popup><b>{item.zoneGroup}</b><br/>Boundary v{item.boundaryVersion}<br/>Effective {item.effectiveDate}</Popup></Polygon>)}

export function BoundaryManager({resources,data,busy,act}){const{t}=useI18n(),[selectedZone,setSelectedZone]=useState(resources.zoneGroups.find(zone=>zone.isActive)?.id||''),[drawing,setDrawing]=useState(false),[points,setPoints]=useState([]),[centerLat,setCenterLat]=useState(''),[centerLng,setCenterLng]=useState(''),[effectiveDate,setEffectiveDate]=useState(today())
  const active=data.items.filter(item=>item.isActive),history=data.items.filter(item=>item.zoneGroupId===Number(selectedZone)),current=active.find(item=>item.zoneGroupId===Number(selectedZone))
  const editCurrent=()=>{setPoints(current?.polygon||[]);setCenterLat(current?.centerLatitude??'');setCenterLng(current?.centerLongitude??'');setEffectiveDate(today());setDrawing(true)}
  const save=()=>{if(points.length<3&&!centerLat){alert('Draw at least three boundary points or enter a valid Zone centre.');return}act(`/api/zone-groups/${selectedZone}/boundaries`,{polygon:points,centerLatitude:centerLat===''?null:Number(centerLat),centerLongitude:centerLng===''?null:Number(centerLng),effectiveDate}).then(()=>{setDrawing(false);setPoints([])})}
  return <><section className="boundary-toolbar"><div><label>Edit Zone<select value={selectedZone} onChange={event=>setSelectedZone(Number(event.target.value))}>{resources.zoneGroups.filter(zone=>zone.isActive).map(zone=><option data-i18n-raw key={zone.id} value={zone.id}>{zone.name}</option>)}</select></label><label>Effective Date<input type="date" value={effectiveDate} onChange={event=>setEffectiveDate(event.target.value)}/></label><label>Center Latitude<input type="number" step="any" value={centerLat} onChange={event=>setCenterLat(event.target.value)}/></label><label>Center Longitude<input type="number" step="any" value={centerLng} onChange={event=>setCenterLng(event.target.value)}/></label></div><div><button onClick={()=>{setPoints([]);setDrawing(true)}}>Draw new boundary</button><button onClick={editCurrent}>Edit current boundary</button><button disabled={!drawing||!points.length} onClick={()=>setPoints(items=>items.slice(0,-1))}>Undo last point</button><button disabled={busy||(!points.length&&!centerLat)} onClick={save}>Save new version</button><button onClick={()=>{setDrawing(false);setPoints([])}}>{t('common.cancel')}</button></div><p>{drawing?'Drawing mode: click the map in order to build a polygon; undo or copy the current version before editing.':'View mode: select a Zone to create or edit a boundary.'} Saving a new version recalculates recommendations without changing official assignments.</p></section><div className="boundary-layout"><div className="zone-map"><MapContainer center={[1.55,110.35]} zoom={9} scrollWheelZoom><TileLayer attribution='&copy; OpenStreetMap contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/><BoundaryLayers items={active}/>{points.length>=2&&<Polygon positions={points.map(point=>[point.lat,point.lng])} pathOptions={{color:'#df4f3f',dashArray:'6 5',fillOpacity:.08}}/>}{data.uncoveredBranches.map(branch=><CircleMarker key={branch.id} center={[branch.latitude,branch.longitude]} radius={3} pathOptions={{color:'#c94955'}}><Popup><span data-i18n-raw>{branch.branchName}</span><br/>Not covered by any Zone polygon</Popup></CircleMarker>)}<MapClicker enabled={drawing} onPoint={point=>setPoints(items=>[...items,point])}/></MapContainer></div><aside className="boundary-side"><div className="boundary-alerts"><article className={data.overlaps.length?'danger':''}><b>{data.overlaps.length}</b><span>Boundary overlaps</span></article><article className={data.uncoveredBranchCount?'warn':''}><b>{data.uncoveredBranchCount}</b><span>Uncovered official GPS Branches</span></article><article><b>{points.length}</b><span>Draft boundary points</span></article></div>{data.overlaps.length>0&&<section><h3>Boundary Conflict</h3>{data.overlaps.map(item=><p data-i18n-raw key={`${item.firstBoundaryId}-${item.secondBoundaryId}`}>{item.firstZone} ↔ {item.secondZone}</p>)}</section>}<section><h3>Draft coordinates</h3><div className="boundary-points">{points.length?points.map((point,index)=><div key={`${point.lat}-${point.lng}-${index}`}><span>{index+1}. {point.lat}, {point.lng}</span><button onClick={()=>setPoints(items=>items.filter((_,itemIndex)=>itemIndex!==index))}>{t('common.remove')}</button></div>):<p>No coordinates drawn.</p>}</div></section><section><h3>Version history</h3>{history.length?history.map(item=><p key={item.id}>v{item.boundaryVersion} · {item.effectiveDate} · {item.isActive?'Active':'Historical'} · {item.polygon?.length||0} points</p>):<p>This Zone has no boundary or centre.</p>}</section></aside></div></>}

function RecommendationManager({resources,boundaries,data,busy,act,t}){const[search,setSearch]=useState(''),[status,setStatus]=useState('pending'),[confidence,setConfidence]=useState(''),[conflictOnly,setConflictOnly]=useState(false),[selected,setSelected]=useState(null),[otherZone,setOtherZone]=useState(''),[otherArea,setOtherArea]=useState('')
  const items=useMemo(()=>data.items.filter(item=>(!search||`${item.branchName} ${item.branchId} ${item.address}`.toLowerCase().includes(search.toLowerCase()))&&(!status||item.status===status)&&(!confidence||item.confidence===confidence)&&(!conflictOnly||item.boundaryConflict)),[data.items,search,status,confidence,conflictOnly]);const areas=resources.areas.filter(area=>!otherZone||(area.confirmedZoneGroupId||area.zoneGroupId)===Number(otherZone))
  const decide=(action,payload={})=>{if(!selected)return;act(`/api/gps-zone-recommendations/${selected.id}/decision`,{action,confirmedBy:'Supervisor',...payload}).then(()=>setSelected(null))}
  return <><div className="recommendation-summary"><article><b>{data.summary.pending||0}</b><span>Awaiting Supervisor</span></article><article><b>{data.summary.highConfidencePending||0}</b><span>High-confidence pending</span></article><article className="danger"><b>{data.summary.conflicts||0}</b><span>Boundary Conflict</span></article><article className="warn"><b>{data.summary.outside||0}</b><span>Outside Polygon</span></article><article><b>{data.summary.noGps||0}</b><span>No official GPS</span></article></div><div className="recommendation-actions"><input placeholder="Search Branch / BranchID / address" value={search} onChange={event=>setSearch(event.target.value)}/><select value={status} onChange={event=>setStatus(event.target.value)}><option value="pending">Awaiting Supervisor</option><option value="later">Review later</option><option value="accepted">Accepted</option><option value="kept_original">Keep original assignment</option><option value="selected_other">Select another</option><option value="no_gps">No official GPS</option><option value="">All statuses</option></select><select value={confidence} onChange={event=>setConfidence(event.target.value)}><option value="">All confidence levels</option><option value="high">High</option><option value="medium">Medium</option><option value="low">Low</option><option value="none">None</option></select><label><input type="checkbox" checked={conflictOnly} onChange={event=>setConflictOnly(event.target.checked)}/> Overlap conflicts only</label><button disabled={busy} onClick={()=>act('/api/gps-zone-recommendations/recalculate',{})}>Recalculate recommendations</button><button disabled={busy||!data.summary.highConfidencePending} onClick={()=>confirm('Accept all High Confidence recommendations that have no conflict and include a suggested Area?')&&act('/api/gps-zone-recommendations/bulk-confirm-high',{confirmedBy:'Supervisor'})}>Bulk confirm High</button></div><div className="recommendation-layout"><div className="recommendation-list"><header><span>{t('gps.itemsShown',{count:items.length})}</span><small>{t('gps.recommendationOnly')}</small></header>{items.map(item=><button key={item.id} className={`${item.boundaryConflict?'conflict ':''}${selected?.id===item.id?'selected':''}`} onClick={()=>setSelected(item)}><span data-i18n-raw><b>{item.branchName}</b><small>{item.branchId} · {item.address||t('common.noAddress')}</small></span><span data-i18n-raw><b>{item.currentArea||t('common.unassignedArea')} → {item.recommendedArea||'Supervisor selection pending'}</b><small>{item.currentZone||'No Zone'} → {item.recommendedZone||'No single suggested Zone'}</small></span><i className={`confidence-${item.confidence}`}>{item.boundaryConflict?'CONFLICT':item.confidence.toUpperCase()}</i></button>)}</div>{selected&&<aside className="recommendation-detail"><header><div><small>GPS RECOMMENDATION</small><h2 data-i18n-raw>{selected.branchName}</h2><p data-i18n-raw>{selected.address||t('common.noAddress')}</p></div><button onClick={()=>setSelected(null)}>{t('common.close')}</button></header><div className="recommendation-map"><MapContainer key={selected.id} center={[selected.latitude,selected.longitude]} zoom={13}><TileLayer attribution='&copy; OpenStreetMap contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/><BoundaryLayers items={boundaries.items.filter(item=>item.isActive)}/><CircleMarker center={[selected.latitude,selected.longitude]} radius={8} pathOptions={{color:'#d6414f',fillOpacity:.9}}><Popup><span data-i18n-raw>{selected.branchName}</span><br/>Official GPS</Popup></CircleMarker></MapContainer></div><div className="recommendation-grid"><span><b data-i18n-raw>{selected.currentArea||'—'}</b>{t('gps.currentArea')}</span><span><b data-i18n-raw>{selected.currentZone||'—'}</b>{t('gps.currentZone')}</span><span><b data-i18n-raw>{selected.recommendedArea||'Pending selection'}</b>{t('gps.suggestedArea')}</span><span><b data-i18n-raw>{selected.recommendedZone||'Conflict / review'}</b>{t('gps.suggestedZone')}</span><span><b>{selected.confidence}</b>Confidence</span><span><b>{selected.matchType}</b>Calculation</span><span><b>{selected.nearestDistanceM==null?'—':`${Math.round(selected.nearestDistanceM)}m`}</b>{t('gps.referenceDistance')}</span><span><b>{selected.boundaryConflict?t('common.yes'):t('common.no')}</b>{t('gps.boundaryConflict')}</span></div><section className="reason-box"><h3>{t('gps.reason')}</h3><p>{selected.reason.summary}</p>{selected.reason.nearestCustomer&&<p data-i18n-raw>Nearest customer: {selected.reason.nearestCustomer.branchName} · {selected.reason.nearestCustomer.distanceMeters}m</p>}<ul>{selected.reason.signals.map(signal=><li key={signal}>{signal}</li>)}</ul></section><div className="decision-actions"><button disabled={busy||!selected.recommendedAreaId} onClick={()=>decide('accept')}>Accept recommendation</button><button disabled={busy} onClick={()=>decide('keep_original')}>Keep original assignment</button><button disabled={busy} onClick={()=>decide('later')}>Review later</button></div><div className="other-assignment"><h3>Select another Zone / Area</h3><select value={otherZone} onChange={event=>{setOtherZone(event.target.value);setOtherArea('')}}><option value="">Select Zone</option>{resources.zoneGroups.filter(zone=>zone.isActive).map(zone=><option data-i18n-raw key={zone.id} value={zone.id}>{zone.name}</option>)}</select><select value={otherArea} onChange={event=>setOtherArea(event.target.value)}><option value="">Select Area</option>{areas.map(area=><option data-i18n-raw key={area.id} value={area.id}>{area.name}</option>)}</select><button disabled={busy||!otherZone||!otherArea} onClick={()=>decide('select_other',{zoneGroupId:Number(otherZone),areaId:Number(otherArea)})}>Confirm other assignment</button></div></aside>}</div></>}
