export const V38_VERSION=38
const counts=db=>({zones:db.prepare('SELECT COUNT(*) n FROM zone_groups').get().n,areas:db.prepare('SELECT COUNT(*) n FROM areas').get().n,vehicles:db.prepare('SELECT COUNT(*) n FROM vehicles').get().n,dispatches:db.prepare('SELECT COUNT(*) n FROM dispatches').get().n,stops:db.prepare('SELECT COUNT(*) n FROM dispatch_stops').get().n,defaults:db.prepare('SELECT COUNT(*) n FROM zone_default_vehicles').get().n})
export function ensureV38Schema(db){db.exec(`CREATE TABLE IF NOT EXISTS route_templates(id INTEGER PRIMARY KEY AUTOINCREMENT,zone_group_id INTEGER NOT NULL UNIQUE REFERENCES zone_groups(id),version INTEGER NOT NULL DEFAULT 1,is_active INTEGER NOT NULL DEFAULT 1 CHECK(is_active IN(0,1)),created_by TEXT,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_by TEXT,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP);CREATE TABLE IF NOT EXISTS route_template_areas(route_template_id INTEGER NOT NULL REFERENCES route_templates(id) ON DELETE CASCADE,area_id INTEGER NOT NULL REFERENCES areas(id),vehicle_id INTEGER NOT NULL REFERENCES vehicles(id),area_order INTEGER NOT NULL CHECK(area_order>0),PRIMARY KEY(route_template_id,area_id),UNIQUE(route_template_id,vehicle_id,area_order));CREATE TABLE IF NOT EXISTS route_template_branches(route_template_id INTEGER NOT NULL REFERENCES route_templates(id) ON DELETE CASCADE,area_id INTEGER NOT NULL REFERENCES areas(id),branch_id INTEGER NOT NULL REFERENCES branches(id),branch_order INTEGER NOT NULL CHECK(branch_order>0),PRIMARY KEY(route_template_id,branch_id),UNIQUE(route_template_id,area_id,branch_order))`)}
export function applyV38Migration(db){const version=Number(db.prepare('SELECT COALESCE(MAX(version),0) version FROM schema_meta').get().version);if(version>=V38_VERSION)return{schemaVersion:version,noOp:true,before:counts(db),after:counts(db)};if(version!==37)throw new Error(`Schema v37 is required before v38; current schema is v${version}`);if(db.prepare('PRAGMA integrity_check').get().integrity_check!=='ok')throw new Error('Database integrity check failed before v38 migration');const before=counts(db);db.exec('BEGIN IMMEDIATE');try{ensureV38Schema(db);if(db.prepare('SELECT COUNT(*) n FROM route_templates').get().n!==0)throw new Error('v38 must not create Route Templates automatically');db.prepare('INSERT INTO schema_meta(version) VALUES(?)').run(V38_VERSION);const after=counts(db);if(JSON.stringify(before)!==JSON.stringify(after))throw new Error('Protected master or Dispatch counts changed during v38 migration');db.exec('COMMIT');return{schemaVersion:V38_VERSION,noOp:false,before,after}}catch(error){db.exec('ROLLBACK');throw error}}
