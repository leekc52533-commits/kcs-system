# KCS Dispatch System — confirmed standing requirements

These requirements were confirmed by KC on 2026-09-09. Carry them forward for later work.

- Before implementing a new system change, restate the requested behavior in plain Chinese and wait for KC to confirm the interpretation. After confirmation, proceed with that scope without asking again. The supervisor overview and desktop/mobile language work in this change are already confirmed.
- Supervisors enter the management interface at Overview (总览), not the last-used dispatch page. Keep normal navigation usable after entry.
- Desktop and employee mobile interfaces must support complete English, Bahasa Melayu and Chinese. The selected language applies to pages, cards, buttons, dropdown labels/options, dialogs, tooltips, validation/errors and success notices. Translate display labels, never API enum values or stored business data.
- Preserve official customer, branch, employee, vehicle, route, area and place names. Address/place data remains in English or Bahasa Melayu; do not translate it into Chinese.
- Data-backed dropdowns must read current master records instead of hard-coded option lists. New saved routes, customers, branches, vehicles, staff and areas appear in relevant selectors, subject to permissions and eligibility. Renames refresh the display. Inactive records are unavailable for new assignments while historical references remain intact.
- This document records development requirements, not a claim that every legacy screen already complies. When changing a screen, verify its affected controls against these rules and report remaining limitations honestly.

- All existing and future photo entry points must use the shared in-page camera: live capture, preview before explicit submission, retake/remove, gallery selection, compression, and retained pending photos on upload failure. Camera buttons and notices support en/ms/zh. Preserve PDF document uploads alongside photo controls. This applies to mobile/office expenses, GPS, weighing, payment/No Goods proofs, employee documents and vehicle records. KC confirmed this scope on 2026-09-09.

- KC confirmed a route-order trial for 2026-09-10 through 2026-09-23 inclusive (Asia/Kuching). Assigned drivers/crew can reorder untouched stops within their running trip; the server disables this exception from 2026-09-24. Retain arrival, bill, completion and pending supervisor-approval protections. Employees may request a future collection date; only supervisors approve it and select the target route. Preserve original records and fixed recurrence, prevent duplicate branch/date stops, and audit requests, decisions and trial order changes.

- KC confirmed a single Employee & Account workspace, with Current and Former employee groups (default Current). Expand the complete employee and login editor directly below the selected name; retain the viewport-bottom horizontal scrollbar. Create an optional login together with a new employee atomically. Keep employment and account state distinct: departure immediately disables login and revokes all existing sessions while preserving history. An account cannot be reactivated while the employee remains departed; rehire is an explicit process.

- Expenses: desktop and mobile share vehicle, odometer km, invoice/reference, merchant company, TIN and remarks fields. Fuel, Services, Repair, Spare Parts, Road Tax, Puspakom and Insurance require all except TIN/remarks. All categories, including Other, require merchant company and invoice/reference, displayed below Other description and outside Receipt details. Other permits blank extra description, vehicle, odometer, TIN and remarks; amount and receipt photo remain required. Receipt OCR fills only empty fields and requires user review before saving; never guess missing values or overwrite manual entries. Preserve original proof and manual entry on recognition failure.

- Every Expenses and Purchase Bill Records column menu supports checkbox multi-selection, Select all, Blank, ascending/descending and clearing filters/sort. Dates sort chronologically, amounts numerically, and names/identifiers naturally (A–Z, embedded numbers). Export uses the same filters/order as the displayed archive.

- Purchase Bill Void: mobile More and desktop beside Purchase Bills. Original issuer requests with a reason; supervisors approve/reject. Preserve original bills/items/photos and audit every decision. Approved void reverses only an existing matching Cash Float deduction, exactly once. Keep completed stops completed. Reissue uses a new bill number linked to the approved void and current eligible product prices, with new Cash payment proof. Never delete the original bill or refund an amount without an original recorded deduction.

- Employee & Account directory: all eleven column menus use the same search, checkbox multi-select, Blank, Select all, ascending/descending, clear sort/filter and Close controls as the archives. Open one menu at a time. Unchecking all means no matches; clearing a filter restores all. Use actual records in the current employee group for options; preserve raw names/identifiers. Former employee dates show the latest employment period.

- Supervisor date requests: expand inline, edit date or retain it and change Route, and choose once-only (default) or permanent. Once-only changes appear as Customer Schedule exceptions; permanent changes update fixed weekdays/Route and reconcile this customer's generated future plans. Preserve executed/protected records and explicitly report their dates. Require an eligible assigned, unstarted target vehicle; expose date-specific routes and a planner entry for missing dates. Keep the original requested date, source stop and review audit.

- KC confirmed on 2026-09-10: office and management roles share dispatch/date/route editing and request decisions without extra permission toggles. Driver/crew still request date/route changes; the existing temporary order trial remains. Future dates are automatically generated on confirmed changes, and active routes remain selectable without an assigned vehicle. Unassigned changes wait for vehicle assignment. Assigned staff can read their unapproved plans; execution still requires departure approval. Existing executed/documented records and duplicate guards remain protected. This supersedes the earlier target-vehicle prerequisite.

- KC confirmed on 2026-09-10: today's assigned driver/crew may report No Goods directly beside each customer without arrival or stop-order prerequisites. Require contact method, reason and proof; skip immediately without approval. Move to a collapsed No Goods section, retain proof/audit, exclude from collected count, and leave fixed recurrence unchanged. Office/management can restore today's records with a reason. Never erase bills or fabricate arrival evidence.

- KC confirmed Sales settlement recording: supervisor receives factory settlements the following day; office/management only, no driver/crew entry. Use the archive toolbar, inline date range, checkbox/blank/sort column menus and bottom scrollbar. Photo OCR fills a review draft; preserve the original, require human verification, keep delivery date per TN slip separate from settlement date, and permit different per-line/factory prices. Factory/vehicle selectors use current masters. Prevent duplicate factory settlement numbers. Settlement recording is not payment confirmation. Raw delivered weight/deductions require separate weighbridge data and are not inferred from settlement weight.

- KC confirmed company-wide sidebar ordering: only his designated account ID may save ordering, not other Admin roles. Pin the existing kcadmin ID once; preserve the employee role label. Group Purchase, Sales, Expenses and Void under Documents, Cash Float outside. Allow desktop dragging and mobile up/down, reset defaults and audit writes. Preserve existing page access; only active leaf is teal. Ownership transfer/recovery is deferred.

- KC confirmed the project-wide download and table-filter presentation standard on 2026-09-10. Apply it to future development and when updating existing pages; this rule supersedes earlier instructions to keep export date inputs permanently inline:
  - All download entry points use a compact white button with a pale teal rounded border and dark teal downward-arrow/tray icon, rather than a large text button. Place it beside the title of the table/card it exports. Provide a translated accessible name and tooltip in en/ms/zh.
  - For date-dependent downloads, clicking the icon expands Start Date ～ End Date controls. The user chooses one day (same start/end) or a date range and explicitly clicks Download. Close the date-selection panel after initiating download. Do not export merely when a date changes. Keep export selection separate from viewing selection; preserve the displayed view.
  - Table exports must honor the chosen export dates plus applicable current column filters and sorting. Cash Float Ledger's export belongs beside its own title and exports ledger data; Daily Records' export is separate and exports its daily summaries. No unexplained changes to export contents.
  - All table search/filter dropdowns use the same header ▼ panel: search options, ascending, descending, clear sort, checkbox multi-selection, Select all, Blank, clear filter and Close. Use live data options; only one panel opens at a time. Keep it open during multi-selection. Dates sort chronologically, amounts numerically, and names/identifiers naturally by letters and embedded numbers. Clearing filters restores all; unchecking all returns no matches.
  - This table-filter rule does not turn single-value form inputs into multi-select controls. Daily Records' single-date viewer keeps its confirmed behavior: selecting a date closes both the dropdown and the Daily Records card and displays that day's summary.
  - Cash Float Ledger follows this standard: title download icon with separate export dates, shared filters on every column, and no redundant standalone Employee selector. Its workbook contains ledger transactions; Daily Records exports summaries separately.

- KC confirmed continuous table headers on 2026-09-10: match Purchase Bill Records with one pale grey-blue header band, slate bold labels and adjacent ▼. Filter triggers are transparent, borderless, without individual boxes, shadows or rounded tiles. Inherit the header font and spacing; keep visible keyboard focus and active-filter indication. Apply this shared style to future and updated table headers while preserving search, multi-select and sorting.

- KC confirmed independent route withdrawal: show Withdraw on each approved route without waiting for the whole day; withdraw only that route with a reason and audit, preserve all stop ordering and other route approvals. Released/executed routes remain protected. Planner up/down ordering applies only to the selected date and route, not the weekday master or other dates; regeneration must preserve both saved route order and execution order.

- KC explicitly authorized committing and pushing KCS changes to this repository's main branch on 2026-09-11; do not repeat push-permission requests for authorized KCS work. This does not replace confirmation of a new product requirement.
- KC confirmed temporary customer collection on 2026-09-11: driver More entry captures customer name and GPS (phone optional), supports on-site billing before customer classification approval, and automatically queues the issued bill for office/supervisor review. Review chooses formal customer, one-time customer, or links an existing branch. Preserve bill/payment/vehicle/driver snapshots and review audit; create no recurring schedule automatically. Restrict field execution to the assigned running trip, retain live arrival GPS and Cash proof requirements, and never bypass an outstanding return-later approval.
- KC requested one unified mobile Temporary Customer page on 2026-09-11: keep one More entry, one shared customer-details/GPS draft, and choose on-site billing or a collection request there. Do not restore a separate legacy new-customer form or require duplicate GPS capture; reuse a still-fresh captured reading while retaining arrival validation and crew restrictions.

- KC confirmed existing-customer lookup in the unified mobile intake page on 2026-09-11: substring company/branch lookup, reuse existing master details/GPS/prices, one-time collection without changing recurrence. Reuse today's same-car stop without duplication; employee requests another car's untouched stop and office/supervisor must approve before moving it. Revalidate source work, target driver/vehicle and date atomically; preserve stop identity, documents and audit. Never transfer arrived, billed or completed stops.

- KC confirmed on 2026-09-13: from 2026-09-14 00:00 Asia/Kuching, driver/crew order edits require supervisor approval; end the free-order trial on September 13. Date/route/vehicle transfer and return-later changes remain subject to approval. Retain temporary customer and once-only pickup entry. No Goods before verified arrival must remain pending without skipping until management approval; verified on-site arrival plus proof may complete immediately. Preserve original assignment until approval, revalidate stale work and ownership, retain proofs and decision audit, and leave normal billing/weight/proof entry available.

- KC confirmed Employee Notice Board on 2026-09-13: management publishes to all currently active employees or explicitly selected active employees. Mobile pages show unread notices on entry and while open; only explicit acknowledgement marks them read, persistently across devices. Retain all received notices under More and show management per-recipient read/unread timestamps. Preserve publication content and recipient snapshots, prevent cross-employee access and duplicate publishes, and support en/ms/zh controls while retaining message text as authored.

- KC confirmed moving notice management from Overview into its own black-sidebar Notice Board entry. Published notices use the shared archive table with date, title, priority, publisher, recipient/read/unread counts; title expands content and receipts inline. Every column keeps shared search/checkbox/Blank/sort/clear controls. Preserve existing company menu order while inserting the new entry, and retain employee popup and More history behavior.

- KC confirmed permanent mobile User Guide on 2026-09-13: from 2026-09-14 00:00 Asia/Kuching show first-use guidance on opening any employee page, explicitly acknowledge once per employee/version across devices, including later hires. Keep full daily steps and approval rules permanently under More → User Guide, state this location in the popup, and provide contextual next-step hints without changing execution permissions. Guide precedes unread notices, which resume after acknowledgement. Support en/ms/zh.

- KC confirmed supervisor employee-phone preview on 2026-09-13: add User Guide beside the Acting Collector title and replace list-only inspection with the same mobile components used by the selected active driver/crew. Preserve vehicle/employee selection and separate explicit takeover. Show Today/Tomorrow, Weight, More, actual saved language and current scoped records/action availability; refresh reloads current data. Preview is read-only in both UI and API, does not acknowledge notices/guide, change assignments, submit records or capture GPS/photos. Clearly distinguish server-record preview from live phone screen/GPS/unsent drafts.

- KC confirmed compact navigation on 2026-09-13: consistent blue file icons for leaf pages, yellow folder icons for expandable groups, indented children and a pale selected-row background. Reduce menu row padding, line spacing and icon/text gaps while retaining names and touch targets. Desktop scroll thumbs appear on hover/focus without layout jumps; touch scrolling remains available. Shared table scrolling displays only one slim bottom horizontal control, suppresses duplicate native table bars, supports keyboard/touch and picks the visible hovered/focused table. Preserve company menu ordering, permissions and existing groups.

- KC confirmed light sidebar and owner-managed folders on 2026-09-13: sidebar pale grey-blue slightly darker than workspace, dark text and a deeper selected row. Pinned menu owner may create/rename one-level folders, move every existing page between top level/folders and reorder; keep all pages exactly once, authorization filters, existing order, revision conflict checks and audits. Menu editor closes on backdrop or Escape; unsaved changes require discard confirmation, saving blocks dismissal. Mandatory employee notice/guide acknowledgements retain their existing rules.

- KC confirmed reference palette update on 2026-09-13: sidebar uses the supplied pale blue (#e8f3fe); file icons use reference purple (#8b70f3 with #b3a3ff accent). Keep yellow folder icons, existing compact spacing and menu behavior. This supersedes the prior grey-blue sidebar and blue file palette.

- KC confirmed hierarchy colors on 2026-09-13: restore sidebar to prior pale grey-blue (#e5e9ef); top-level file icons stay purple, child-page icons inside every folder use light blue (#75bce8, accent #b9e3fa), and folders remain yellow. Apply the same distinction in the menu editor; moving a page changes its icon according to its current level. This supersedes the all-purple files and pale-blue sidebar palette.

- Sidebar scrollbar fix confirmed by reported behavior on 2026-09-13: retained mouse-click focus must not keep the scrollbar visible after pointer leave. Use hover or keyboard focus-visible, never broad focus-within; retain keyboard focus indication and scrolling.

- KC confirmed on 2026-09-14: Office, Supervisor, Operations Admin and Owner Admin may read all company purchase, sales, expense and void records with related proofs, including replacement bills. Preserve existing mutation, original-issuer and approval checks; driver/crew access remains scoped.

- KC confirmed on 2026-09-14 that the Expense Records column arrangement is the standing pattern for future and updated KCS tables. Users can choose the placement/order of column headings and their corresponding cells through a compact toolbar column-arrangement entry. Press-and-hold dragging up/down is the primary reorder interaction in that editor; up/down buttons remain a keyboard/touch fallback, not the only way to move a column. Provide explicit Save and Restore default, preserve saved column order, keep header/data alignment and existing filters/sorting/permissions intact, and append newly introduced columns without losing existing saved choices. Clearly state the persistence scope: the current Expense Records implementation saves in the current browser, not across all accounts/devices. Follow docs/UI_GUIDELINES.md for this pattern. This records a future development standard, not a claim that all legacy tables already support it; apply it whenever creating or modifying a comparable table.

- KC confirmed short document numbering on 2026-09-14 for newly created documents: Purchase PYYMMDD-NNN, Sales SYYMMDD-NNN, Expenses EYYMMDD-NNN, Void requests/records VYYMMDD-NNN (e.g. E260914-001). Use actual issue time in Asia/Kuching, with an independent company-wide daily sequence for each prefix; employee and admin expenses share E. Allocate atomically with document creation, prevent concurrent duplicates, preserve numbers on retries/edits/corrections, never reuse an issued number, and continue beyond 999 without truncation. Leave legacy numbers unchanged. S is an internal KCS number; preserve the factory settlement/invoice reference and duplicate checks separately. V links to the original purchase bill and its approval status; it does not imply approval.

- KC confirmed Trip Exception Handling on 2026-09-14: office/dispatch management entry in Dispatch & Collection Schedules, date-scoped running trips including all hidden branch stops; show blockers and next steps. Management can cancel only closed, untouched, unprotected stops with a required reason, and finish a running trip only after all stops are completed/cancelled, with a reason. Revalidate atomically, preserve all evidence/history, audit actual operator, and keep driver/crew mutations denied in this management endpoint. This does not enable arbitrary stop cancellation or mark uncollected work as collected.
